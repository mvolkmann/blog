name: Shell Commands
on: [push]
jobs:
  build:
    name: ShellJob
    runs-on: ubuntu-latest
    steps:
      - id: working-dir
        run: pwd
      - id: list-files
        run: ls -l
      - id: date
        run: date
